# Optional heavy dependencies for running Gemma locally via HuggingFace transformers.
# Not required for the Ollama backend or the offline mock backend.
#
# Gemma 3n (multimodal, used by the video feature) requires transformers >= 4.53.
transformers>=4.53.0
torch>=2.2.0
accelerate>=0.30.0
pillow>=10.0.0
